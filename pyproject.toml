[project]
name = "mnist-pruning"
version = "0.1.0"
description = "CNN MNIST pruning using gradient-based importance"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "matplotlib>=3.7.0",
]
